server:
  port: 8083

spring:
  application:
    name: telegram-bot
  redis:
    host: ${project.variables.redis.host}
    port: ${project.variables.redis.port}

telegram:
  api:
    path: "https://api.telegram.org/file/bot"
